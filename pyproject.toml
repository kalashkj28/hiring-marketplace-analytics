[project]
name = "hiring-marketplace-analytics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kalash Jain", email = "kalashjainkj28@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "pandas>=3.0.6",
    "plotly>=7.1.0",
    "streamlit>=1.64.0",
]

[project.scripts]
hiring-marketplace-analytics = "hiring_marketplace_analytics:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
